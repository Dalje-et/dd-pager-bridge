# MQTT (HiveMQ Cloud)
MQTT_BROKER=your-cluster.s1.eu.hivemq.cloud
MQTT_PORT=8883
MQTT_USER=your-mqtt-user
MQTT_PASS=your-mqtt-pass

# Datadog API (fallback — per-device keys are stored in SQLite via /setup page)
DD_API_KEY=your-dd-api-key
DD_APP_KEY=your-dd-app-key
DD_SITE=datadoghq.com

# Bridge URL (used for webhook auto-creation)
BRIDGE_URL=https://dd-pager-bridge.onrender.com

# Device (default device ID for legacy single-device mode)
DEVICE_ID=ddpager-poc-001
